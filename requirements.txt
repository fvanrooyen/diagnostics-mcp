# Core runtime: MCP servers + OAuth + HTTP serving
mcp==1.27.2
cryptography==49.0.0          # tls_server, oauth_server (cert parsing, JWT signing)
paramiko==5.0.0               # ssh_diag_server
starlette==1.3.1              # oauth_server, serve_http_auth
python-multipart==0.0.32      # oauth_server form parsing (/authorize, /token)
uvicorn==0.49.0               # HTTP serving
PyJWT==2.13.0                 # oauth_server, mcp_auth
psycopg[binary,pool]==3.3.4   # oauth_server PostgresStore (set DATABASE_URL)

# Multi-agent orchestrator (optional — only needed for triage_agent.py)
# claude-agent-sdk==0.2.101

# Dev/verification (optional)
# pytest                       # tests/ (Store adapters; set TEST_DATABASE_URL for the Postgres backend)
# httpx==0.28.1
# pyyaml ; python-hcl2
